/**
 * B11 : le paramétrage de l'activité.
 *
 * Plus aucune navigation propre. Décision D12, et planche 14c : **le hub EST la
 * navigation du paramétrage**. Chaque rangée de « Ton activité » résume sa
 * section et l'ouvre ; une barre d'onglets par-dessus ferait deux navigations
 * concurrentes pour cinq écrans, et c'est cette colonne latérale, que personne
 * n'avait dessinée, qui a fait marquer deux passages cassés à la recette 6.
 *
 * Ce fichier subsiste pour que le segment de route reste explicite, et pour
 * porter cette explication là où quelqu'un la cherchera.
 */
export default function LayoutParametrage({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
